CREATE TABLE ALLOCATAIRES (
                              NUMERO BIGINT PRIMARY KEY,
                              NOM VARCHAR(255),
                              PRENOM VARCHAR(255),
                              NO_AVS VARCHAR(20)
);
